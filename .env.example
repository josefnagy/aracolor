PORT=3000
JWT_SECRET=change-this-to-a-random-secret
NODE_ENV=production
# Set to 'true' only if the admin is served over HTTPS
COOKIE_SECURE=false

# On VM these point to the webroot
WEBROOT=/var/www/aracolor
ARCHIVE_DIR=/var/www/aracolor/archive
DATA_DIR=/var/www/aracolor/data
